// Macro Manager - 用于自定义转义规则
import * as vscode from 'vscode';

export class MacroManager {
    static async executeMacro(): Promise<void> {
        // 实现宏功能
        vscode.window.showInformationMessage('宏功能开发中...');
    }
}